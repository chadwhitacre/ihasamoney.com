from ihazmoney import db, get_tags
^L
if request.POST:
    email = request.user.session['email']
    tag = request.path['tag']
    db.execute("INSERT INTO tags (email, tag) VALUES (%s, %s)", (email, tag))
    out = get_tags(email)
else:
    out = []
response.body = out
